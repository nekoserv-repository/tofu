{
	"email": "{{ lego_email }}",
	"registration": {
		"body": {
			"status": "valid",
			"contact": [
				"mailto:{{ lego_email }}"
			]
		},
		"uri": "https://{{ lego_server }}/acme/acct/{{ lego_acct_id }}"
	}
}